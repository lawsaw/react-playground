import React from 'react';

import {
    // Home,
    // About,
    // Test,
    // Clock,
    // Dashboard,
    // TodoList,
    Tetris,
} from '../route-pages';

export const HOME_PAGE = props => <Tetris {...props} />;
export const IS_SINGLE_PAGE = true;