import React from 'react';

import {
    Home,
    Crocodile,
    // About,
    Test,
    // Clock,
    // Dashboard,
    // TodoList,
    //Tetris,
} from '../route-pages';

export const HOME_PAGE = props => <Home {...props} />;
export const IS_SINGLE_PAGE = false;